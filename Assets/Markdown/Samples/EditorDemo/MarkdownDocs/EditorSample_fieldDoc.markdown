(these docs come from a
[file](EditorSample_fieldDoc.markdown),
by the way). 

The `x` field is a simple property that will control how
many times the `y` field is printed. 

```csharp
for (var i = 0; i < x ; i ++) { 
    // do logic to print y
}
```
