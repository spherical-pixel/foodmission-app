/*
 * Font overrides applied via CSS class on the App UI Panel root.
 * Roboto is the App UI default
 * -unity-font-definition inherits downward to all child elements.
 */

.fm-font-open-sans {
    -unity-font-definition: resource('Fonts & Materials/OpenSans-VariableFont_wdth,wght SDF');
}

.fm-font-open-dyslexic {
    -unity-font-definition: resource('Fonts & Materials/OpenDyslexic-Regular SDF');
}

.fm-font-roboto {
    -unity-font-definition: resource('Fonts & Materials/Roboto-VariableFont_wdth,wght SDF');
}